spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        shared-dataids: printlog.yml,redis.yml
        refreshable-dataids: redis.yml
  profiles:
    active: prod
  application:
    name: pf-push