spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        shared-dataids: printlog.yml,lightmq.yml
        refreshable-dataids: lightmq.yml
  profiles:
    active: prod
  application:
    name: pf-consumer